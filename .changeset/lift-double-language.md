---
"@chbrain/khai-plays-grimm": patch
---

Lift the whole house to the double language. The canon now requires a `declared`
name on every non-English instance (the German house resolves to German), so
every play element now carries its German stage name as `declared` and as its
H1, alongside the English `title` the tooling keys on. The German names are the
ones each play already bills in its Company and Triggers, so the page reads in
German while the schema stays English. Bumps the canon dependencies (khai-arch,
khai-language, khai-tests) to the versions that enforce it. Cinderella's two
plans are genuinely bilingual (`der Gnadenbund am Grab`, `der Aschen-Zwang`),
and its defeated stepmother target carries the canon `[F]` failed verdict.
