---
---

Adopt the registry build-drift gate: bump `@chbrain/khai-tests` to `^0.1.27` to activate the new conformance gate that asserts `registry.json` equals a fresh build. This registry already matches the build (nothing to reconcile), so the bump ships no content: an empty changeset.
